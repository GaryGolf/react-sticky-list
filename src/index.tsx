import * as React from 'react';

interface Props {}

export default class ReactStickyList extends React.PureComponent<Props, null> {
  render() {
    return (
      <h1>React Sticky List Component</h1>
    )
  }
}